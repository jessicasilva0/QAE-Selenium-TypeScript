{
  "scripts": {
    "format": "npx prettier . --write",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && npx mocha dist/tests/specs/*.spec.js --timeout 120000 --parallel --retries 2"
  },
  "dependencies": {
    "dotenv": "^17.2.4",
    "selenium-webdriver": "^4.40.0"
  },
  "devDependencies": {
    "@types/chai": "^5.2.3",
    "@types/mocha": "^10.0.10",
    "@types/node": "^25.2.1",
    "@types/selenium-webdriver": "^4.35.5",
    "chai": "^6.2.2",
    "mocha": "^11.7.5",
    "prettier": "3.8.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
